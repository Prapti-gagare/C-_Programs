#include <iostream>
#include <set>
using namespace std;

int main() 
{
    set<int> a = {1, 2, 3, 4};
    set<int> b = {3, 4, 5, 6};

    cout << "Intersection: ";
    for (int x : a) {
        if (b.find(x) != b.end()) 
        {
            cout << x << " ";
        }
    }

    return 0;
}